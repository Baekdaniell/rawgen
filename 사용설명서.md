# rawgen 사용설명서 (초보자용)

프로그램: `build\bin\rawgen.exe` (GUI 하나로 모든 작업 수행)
버전: 0.3.0 (2026-08-14 — E2E 자동 실행·CLI 폐기·GUI 전면 개선판)

> 참고 1: 프로그램 이름이 `statsgenerator` → **`rawgen`**으로 바뀌었습니다. 설정 폴더도
> `%APPDATA%\statsgenerator\` → `%APPDATA%\rawgen\`으로 바뀌는데, **처음 실행할 때 기존
> 폴더(프로파일·실행 이력·E2E 상태)를 통째로 자동 이관**하므로 따로 할 일은 없습니다.
>
> 참고 2: 예전의 명령줄 도구(statsgen-cli)는 폐기되었습니다. 시나리오 파일 불러오기,
> E2E 자동 실행, 이력 리포트 등 CLI에만 있던 기능이 전부 GUI로 옮겨졌습니다 (§6 참고).

---

## 1. 이 프로그램은 무엇을 하는 물건인가

### 배경 문제

MK119 제품은 장비에서 수집한 값(raw data)을 ClickHouse에 쌓고, 매일 새벽에 그 데이터를 집계해서 "일별/시간별 통계"를 MariaDB에 저장합니다. 사용자가 화면에서 보는 min, max, avg 통계가 바로 이것입니다.

그런데 이 통계가 정확한지 QA로 확인하려면 문제가 있습니다. **통계는 하루가 지나야(D+1 새벽) 만들어지고**, "min이 정확히 20, max가 정확히 40인 데이터"처럼 답을 미리 아는 데이터를 만들 방법도 없습니다.

### 해결 방법: 답안지를 들고 채점하기

1. **내가 답을 정한다**: "이 날짜의 데이터는 min 20, max 40, 평균 30이 되게 해줘"라고 목표 입력
2. **도구가 데이터를 만든다**: 그 목표를 정확히 만족하는 raw data 수천 건 생성
3. **ClickHouse에 주입한다**: 과거 날짜로 batch INSERT (넣은 직후 제대로 들어갔는지 자동 재확인)
4. **제품에게 계산시킨다**: 제품의 과거 통계 재생성을 실행 (절차는 도구가 안내)
5. **채점한다**: 제품이 MariaDB에 저장한 통계를 도구가 읽어와 내 답과 자동 비교

### 검증은 두 층으로 나뉩니다 (화면에 L1/L2로 표시)

| 층 | 언제 | 무엇을 채점 |
|---|---|---|
| **L1** | 주입 직후 (재생성 필요 없음) | ClickHouse 안에서: 내 기대값 ↔ 원본(checkvalue) ↔ 자동 집계(MV)가 일치하는가 |
| **L2** | 제품 재생성 후 | 제품이 MariaDB에 저장한 daily/hourly 통계가 기대값과 일치하는가 (진짜 목적) |

L1이 먼저 통과해야 L2 불일치가 "제품 잘못"이라고 말할 수 있습니다.

---

## 2. 시작 전 준비물

- QA 환경의 **MariaDB 접속 정보** (host, port, DB명, 계정)
- QA 환경의 **ClickHouse 접속 정보** (host, native 포트 9000, DB명, 계정)
- 주입에 쓸 **테스트용 checkpoint id** (운영 데이터와 섞이지 않는 전용 대역 권장)
- 이 PC에서 해당 서버로 3306/9000 포트가 열려 있어야 합니다 (안 열려 있으면 SSH 터널 등 필요)

> 중요: 이 도구는 **TestOnly로 표시한 프로파일에서만 INSERT를 허용**합니다. 운영 DB 정보를 넣더라도 TestOnly를 켜지 않으면 쓰기가 차단됩니다. 반대로 말하면, TestOnly는 "이 DB는 테스트용이 맞다"는 본인 확인입니다. 신중히 켜세요.

---

## 3. 화면 구성 (9단계)

화면은 네 부분입니다 (2026-08-20 개편):

- **상단 스테퍼**: 1~9 단계가 점과 선으로 이어진 가로 진행 표시. 점을 클릭해 이동하고(Ctrl+1~9도 동일),
  완료 단계는 체크, 현재 단계는 파란 점으로 표시됩니다. 단계 라벨은 항상 보이며, 창이 좁으면
  스테퍼 안에서 가로로 스크롤됩니다. 우측 끝 **기어 버튼(설정)** 을 누르면 설정 메뉴가 열리고,
  그 안에서 **테마**(다크/다큘라/미드나이트/라이트)를 고를 수 있습니다. 선택은 저장됩니다.
- **좌측 세션 탐색기**: 등록된 접속 세션 목록. 클릭하면 그 세션으로 전환되며,
  연결 확인 점(CH·MariaDB)과 INSERT 허용 여부가 함께 보입니다. [새 세션]/[복제]도 여기 있습니다.
  **작업 내용(대상 선택·시나리오)은 세션마다 따로 저장**되어 전환하면 그 세션에서 하던 내용이
  복원됩니다. 아직 작업한 적 없는 세션은 빈 기본 시나리오로 새로 시작합니다(다른 세션의
  날짜·checkpoint를 물려받지 않습니다). 이전 세션의 실행 결과(미리보기 확인·검증 결과·완료 체크)는
  다른 서버의 결과이므로 전환 시 초기화됩니다. 앱을 다시 켜면 마지막으로 쓰던 세션과
  그 작업 내용에서 이어집니다. 시나리오를 다른 세션으로 옮기고 싶으면 시나리오 화면의
  JSON 저장/불러오기를 쓰세요.
- **중앙 콘텐츠**: 선택한 단계의 작업 화면.
- **하단 상태 바**: 지금 어느 세션(서버)에 붙어 있는지, INSERT 허용 여부, 실행 상태, 버전.

| 단계 | 하는 일 |
|---|---|
| 1. 연결 | DB 프로파일 생성, 연결 테스트, 스키마 확인 |
| 2. 대상 선택 | 주입할 checkpoint 선택 |
| 3. 시나리오 | 날짜, 주기, 목표 통계값 입력 |
| 4. 미리보기 | 생성될 데이터와 "정답" 미리보기 (DB 미접근) |
| 5. 주입 | ClickHouse에 실제 INSERT + readback |
| 6. 제품 재생성 | 제품 재생성 유도 절차 안내 (체크리스트 생성) |
| 7. 검증 | L1/L2 검증 실행 |
| 8. 온종일 자동 검증 | hourly 온종일 E2E: 주입→시간대별 자동 verify→리포트를 한 번에 |
| 9. 리포트 | 결과를 Markdown/JSON/CSV로 저장 |

---

## 4. 따라하기 A: DB 없이 체험 (5분)

DB 연결 없이도 3~4단계는 동작합니다. 알고리즘과 흐름만 먼저 보려면:

1. **3. 시나리오**로 이동. 기본값이 채워져 있습니다 (어제 날짜, 10초 주기, min 20/max 40/avg 30, 2시 NULL, 15시 override)
2. **2. 대상 선택**에서 checkpoint_id 직접 입력란에 아무 숫자(예: 660070) 넣고 [추가]
3. **4. 미리보기** → [Preview 실행]. 예상 row 수, 날짜별 정답 통계, 시간대별 표가 나옵니다
4. **5. 주입** → [Dry-run]. DB 없이 생성만 수행하고 실행 이력에 기록됩니다

## 5. 따라하기 B: 실제 검증 한 사이클

### 1단계. 연결 (Connections)

1. [새 세션] → 이름 입력 (예: `qa-koscom`)
2. MariaDB / ClickHouse 접속 정보 입력
3. [MariaDB 연결 테스트], [ClickHouse 연결 테스트] 각각 성공 확인
4. 테스트 DB가 맞으면 **TestOnly 체크** 후 [저장]
5. [스키마 확인] 실행 → checkvalue 컬럼, Replicated 여부, exclude_date 등재 현황이 나옵니다. "문제" 항목이 있으면 진행 전에 해결

#### 세션을 여러 개 등록해 쓰기

검증 대상 환경마다 세션을 하나씩 등록해 두고 **좌측 세션 탐색기**에서 골라 씁니다.

- **탐색기 항목**: 세션 이름 / CH 호스트 / 연결 확인 점(CH·MariaDB — 초록=확인, 빨강=실패, 회색=미확인) /
  INSERT 허용 배지. 클릭하면 그 세션으로 전환됩니다. **주입·검증·E2E는 항상 선택된 세션에 실행됩니다.**
- **어느 화면에서든 확인**: 탐색기는 상주 패널이고, 하단 상태 바에도 현재 세션이 항상 표시됩니다.
  주입 직전에 대상이 맞는지 여기서 확인하세요.
- **[복제]**: 같은 서버에 테이블명만 다른 배포본을 등록하거나, 공유받은 세션을 원본 보존한 채 고칠 때.
- **주고받기(JSON)**: [파일로 내보내기]/[클립보드로 내보내기]는 **비밀번호를 자리표시자로 치환**합니다.
  받는 쪽은 [파일에서 가져오기] 또는 붙여넣기 칸을 씁니다. 이름이 같은 세션은 갱신되고, 없으면 새로 생깁니다.
  가져온 세션은 **비밀번호가 비어 있고 TestOnly가 꺼진 채로** 들어옵니다 — 남의 환경 설정으로 INSERT가 열리지 않게 하려는 것이므로, 직접 입력하고 테스트 DB임을 확인한 뒤 켜세요.

### 2단계. 대상 선택 (Targets)

- [조회]로 checkpoint 목록을 불러와 체크하거나, id를 직접 입력해 [추가]
- **전용 QA checkpoint를 쓰세요.** 주입한 (날짜 × cp)는 나중에 다른 검증의 "깨끗한 비교 기준"으로 쓸 수 없게 됩니다 (리포트에 오염 마킹으로 기록됨)

### 3단계. 시나리오 (Scenario)

- 날짜: **오늘 기준 13일 이내** (그보다 과거는 보존 창 만료로 검증 불가, 실행이 차단됩니다)
- 목표 입력 요령은 §7 참고
- 하단의 "시나리오 JSON": [파일로 저장]해 두면 나중에 [불러오기]로 같은 검증(동일 seed → 동일 데이터 → 동일 기대값)을 그대로 재현할 수 있습니다. 손으로 재입력하면 한 글자만 달라도 검증이 조용히 틀어지므로 **재현은 반드시 파일 경유**를 권장합니다
- 마지막으로 편집한 시나리오는 앱을 껐다 켜도 자동 복원됩니다
- 폼 어디서든 Ctrl+Enter를 누르면 바로 Preview가 실행됩니다

### 4단계. 미리보기 (Preview)

- [Preview 실행] → 노란 경고 상자를 반드시 확인 (보존 창 밖, 수렴 실패 등)
- 여기 표시되는 기대값이 곧 채점 답안지입니다

### 5단계. 주입 (Generate)

1. "실행 전 확인" 상자에서 대상 DB, 날짜, row 수 확인
2. 확인 체크박스 체크 → [실제 INSERT 실행] 활성화 (TestOnly 프로파일이어야 함)
3. 진행률 표시, [취소]는 batch 경계에서 반영
4. 끝나면 **readback 결과**를 확인:
   - `적재 확인(N행, 중복 0)` → 정상
   - `적재 N < 계획 M` → **같은 시나리오를 재실행한 경우** ClickHouse가 같은 데이터 블록을 조용히 무시한 것(insert_deduplicate). seed를 바꾸거나 기존 데이터를 확인
   - `완전 동일 행 N건 검출` → 이 범위에 중복이 생김 (기존 데이터와 충돌)

### 6단계. 제품 재생성 (Regenerate)

제품(analyzer)은 이미 통계가 있는 날짜를 다시 만들지 않습니다(멱등 스킵). 그래서 절차가 필요합니다:

- [생성] 버튼 → 대상 날짜/cp가 반영된 체크리스트가 나옵니다 (통계 행 삭제 SQL, liz_log 마커 삭제, ACTIVE/STANDBY 재기동 순서, 주의할 로그 문구)
- **이 도구는 삭제/재기동을 대신 실행하지 않습니다.** 체크리스트를 복사해 작업자가 QA 서버에서 직접 실행하세요
- 함정: 재생성 잡은 당일 수집이 확인 안 되면 30분 대기 후 그냥 종료합니다(verifyAllLogSaved 게이트). 수집이 살아있는 상태에서 실행하세요

### 7단계. 검증 (Verify)

- 주입 직후: [L1만 검증] → 데이터가 제대로 들어갔고 MV 집계가 맞는지
- 재생성 후: [L1 + L2 전체 검증] → 제품 통계까지 채점
- 실행 전 **복제 지연 가드**가 자동 적용됩니다: 지연이 60초를 넘으면 검증을 거부합니다. 지연된 복제본과 비교하면 가짜 불일치가 수십만 건 나온 실사례가 있어서입니다. 잠시 후 재시도하세요
- exclude_date에 등재된 날짜가 끼어 있으면 경고가 뜹니다 (hourly가 다른 경로로 생성되는 날)
- 층 표의 결과는 네 가지입니다: **PASS**(대조했고 일치) / **FAIL**(대조했고 불일치) /
  **ERROR(대조 불가)**(조회 자체가 실패) / **미검증**(대조 대상이 0건 — 예: 보존 창 밖).
  ERROR와 미검증은 "이상 없음"이 아닙니다 — 그 층의 "불일치 0"은 통과가 아니라 안 본 것이라는 뜻입니다.
  `미검증(창 밖)` 열은 보존 창을 벗어나 대조에서 제외한 시간대 수입니다

#### 오늘 주입하고 내일 아침에 검증하기 (자정 전에 PC를 끄는 날)

23시 시간대와 일별 값은 **익일 00:05**에 만들어지므로, 8단계 자동 모드는 자정을 넘겨야 완주합니다.
퇴근 전에 끝내야 하면 이렇게 하세요.

1. 오늘: 3~5단계로 **주입만** 하고 앱 종료 (23시처럼 아직 오지 않은 시각의 행을 미리 넣는 것이 정상입니다)
2. 다음 날 아침: 앱을 켜면 시나리오가 그대로 복원됩니다 → 7단계에서 [L1 + L2 전체 검증] 한 번
3. 결과 읽는 법: **23시 셀과 일별 값이 판정 대상**입니다. 00~22시 시간대는 보존 창(당일 + 전일 23시)을
   이미 벗어나 조회 자체가 불가능하므로 **미검증**으로 표시되는 것이 정상입니다.
   이때 화면이 PASS로 뜨지 않는 이유가 이것입니다 — 대조하지 못한 층은 통과로 세지 않습니다

### 8단계(선택). hourly 온종일 E2E — 한 번에 자동 실행 (8. 온종일 자동 검증)

hourly 검증은 보존 창(당일+전날 23시) 때문에 "H시 통계가 (H+1):05에 생성될 때마다" 확인해야 해서, 원래는 셸에서 generate → 대기 → verify를 온종일 반복해야 했습니다. **E2E 탭이 이걸 한 번에 대신합니다**:

> ⚠️ **주입 시각이 검증 범위를 결정합니다 (가장 중요).** 어떤 시간대(H)의 통계는 **(H+1):05에 딱 한 번** 만들어집니다. 그 시각이 지난 뒤에 주입하면 그 시간대는 **영원히 반영되지 않습니다**(백필은 해당 시간대 행이 통째로 0일 때만 도는데, 운영 중인 환경은 다른 checkpoint 행이 이미 있어 0이 되지 않습니다).
> - 09시에 주입 → 00~07시는 "제외(skip)", **08시부터 판정 가능**
> - **하루 24시간을 다 검증하려면 그날 00:05 이전에 주입**해야 합니다
> - 사전 점검 카드의 "셀 계획(총/제외)"에 몇 개가 제외되는지 미리 나오니 시작 전에 확인하세요

1. Scenario를 **오늘 날짜**로 만든다 (§7의 override로 NULL·상수 시간대 포함 가능)
2. E2E 탭에서 확인 체크 → [E2E 시작]
3. 시작하면 먼저 **사전 점검** 카드가 뜹니다: MariaDB/ClickHouse 연결, TestOnly, 오늘 날짜 여부, 셀 계획(총/제외 개수)과 **예상 종료 시각**. 차단 항목(연결 실패, 전 셀 제외 등)이 있으면 실행되지 않습니다 — 과거 날짜 시나리오로 하루치 INSERT를 낭비하는 실수를 여기서 잡습니다
4. 이후는 자동: 주입(readback 포함) → L1 게이트(불합격이면 즉시 중단 — 주입이 틀렸는데 하루를 기다리지 않도록) → 시간대별 생성 시점((H+1):05 + 여유 10분)에 맞춰 verify 반복 → 셀(cp×날짜×시간) 단위로 판정 누적
5. 불일치가 나온 셀은 재확인 후 확정합니다 (기본 10분 간격 × 3회 — "고급 설정"에서 조정 가능. writer가 쓰는 도중의 부분 노출을 결함으로 오판하지 않기 위해)
6. 23시 셀과 일별(daily) 셀은 다음날 00시대에 판정됩니다. 자정 전에 끝내려면 "일별 셀 제외"를 체크
7. 대기 중에는 상태 배지에 **다음 판정까지 남은 시간이 초 단위로 카운트다운**되고, 셀 테이블 위의 칩(PASS/FAIL/재확인/대기/제외)을 클릭하면 해당 상태만 필터됩니다. [문제만 보기]는 FAIL+재확인만 남깁니다

끝나면:
- 리포트(셀 타임라인 + 불일치 상세 + 최종 verify)가 `%APPDATA%\rawgen\reports\`에 **자동 저장**되고, Windows 토스트 알림이 옵니다. [리포트 MD 저장]으로 원하는 위치에 따로 저장할 수도 있습니다
- 최종 verify 결과는 검증 탭에도 반영됩니다
- **최종 판정은 PASS / FAIL / 미검증(INCONCLUSIVE) 세 가지입니다.** 판정된 셀이 하나도 없거나,
  판정 시각을 놓쳐 대조하지 못한 셀이 남으면 실패가 없어도 PASS가 아니라 "미검증"입니다.
  미검증으로 끝난 실행은 통과로 결재할 수 없고, 재개용 상태도 지우지 않습니다
- 셀 계획 이전에 중단된 저장 상태로는 재개할 수 없습니다(셀 0개로 즉시 완주해 거짓 통과가 되기 때문).
  배너가 그 사실을 알려주고 [이어서 하기]가 잠깁니다 — 처음부터 실행하세요
- 재개는 **저장 상태와 같은 대상 DB**에서만 됩니다. 프로파일을 바꿔 이어받으려 하면 거부합니다

실행 중 보호 장치:
- **시스템 절전 자동 방지** (화면은 꺼져도 됨) — 절전으로 판정 시점을 놓치는 사고 방지
- **실행 중 창을 닫으면 확인 대화상자**가 뜹니다
- E2E 실행 중에는 Generate/Verify 버튼이 잠깁니다 — 같은 (날짜×cp)에 추가 INSERT하면 기대값이 깨져 남은 셀이 전부 오염되기 때문입니다
- **진행 상태가 매 판정마다 저장**됩니다. 앱이 꺼지거나 크래시해도 다시 켜면 E2E 탭 상단에 "이어서 하기" 배너가 떠서, 클릭 한 번으로 시나리오 복원 + 완료된 셀 판정을 유지한 채 재개합니다

기타:
- "제외(skip)" 표시 = 주입 시점에 그 시간대의 생성((H+1):05)이 이미 지나 대조가 불가능한 영역입니다. 과거 날짜는 전 시간대가 여기 해당합니다
- "불합격에도 계속"(keep-going)은 주입이 틀어진 상태에서 시간대별 파이프라인 동작만 관찰하고 싶을 때 쓰는 진단용 옵션입니다. 이 모드의 결과는 리포트에 경고가 박히며 합격 판정 근거로 쓰면 안 됩니다

### 9단계. 리포트 (Report)

- 탭에 들어오면 최신 결과로 자동 생성됩니다. 앱을 껐다 켠 뒤에도 최근 주입 이력이 자동 반영됩니다
- 과거 실행의 리포트는 5. 주입의 실행 이력 테이블에서 해당 행의 [리포트] 버튼으로
- [MD 저장]으로 파일 저장, [복사]로 Jira/Confluence에 붙여넣기. 결과 테이블(기대값·Mismatch·E2E 셀)은 각 패널의 [표 복사]로 탭 구분 텍스트 복사(엑셀/Jira 표 붙여넣기), 헤더 클릭으로 정렬이 됩니다

---

## 6. GUI로 같은 검증 재현하기 (구 CLI 사용자용 이행 안내)

예전 statsgen-cli의 모든 기능은 GUI에 있습니다. 명령별 GUI 위치:

| 구 CLI 명령 | GUI에서 |
|---|---|
| `profiles` | 1. 연결의 프로파일 드롭다운 |
| `test --profile` | [MariaDB 연결 테스트] / [ClickHouse 연결 테스트] 버튼 |
| `inspect --search` | 1. 연결 [스키마 확인] (복제 지연 포함) + 2. 대상 선택 검색 |
| `preview --scenario s.json` | 3. 시나리오 [불러오기] → 4. 미리보기 [Preview 실행] |
| `generate --dry-run / --confirm` | 5. 주입 [Dry-run] / [실제 INSERT 실행] |
| `verify --skip-l2` | 7. 검증 [L1만 검증] |
| `verify --out report.md` | 7. 검증 실행 → 9. 리포트 [MD 저장] |
| `e2e --confirm` | 8. 온종일 자동 검증 [E2E 시작] |
| `e2e --skip-generate` | "주입 생략" 체크 (또는 재개 배너의 [이어서 하기]) |
| `e2e --keep-going / --skip-daily / --retry` | E2E 탭 체크박스 + "고급 설정" |
| `report` | 9. 리포트 (앱 재시작 후에도 최근 이력으로 자동 생성) / 5. 주입 이력의 [리포트] 버튼 |

재현의 핵심은 **시나리오 JSON 파일**입니다: 검증 시점에 [파일로 저장] → 재현 시점에 [불러오기]. 같은 시나리오(같은 seed)면 언제 어디서든 같은 데이터·같은 기대값이 재계산됩니다 (실행 이력에 의존하지 않는 멱등 검증).

---

## 7. 목표값 입력 요령 (Scenario)

### Daily Goal

- min 20 / max 40 / avg 30: 그날 데이터의 최솟값이 정확히 20, 최댓값이 정확히 40, 평균 30
- avg는 min과 max 사이여야 합니다
- 도달 불가능한 목표 주의: min 20 / max 40 / avg 20.01은 max 40 한 건이 평균을 끌어올려 불가능 → Preview에 "수렴 실패" 경고가 뜨고 기대값은 실제 생성값 기준으로 잡힙니다

### Hourly Overrides

- **goal**: 특정 시간대만 다른 min/max/avg (예: 15시만 부하 급증 흉내)
- **NULL**: 그 시간대 데이터를 아예 안 만듦 = 수집 끊김 재현. 제품이 이를 어떻게 저장해야 정답인지가 테이블마다 다릅니다:
  - daily_stats: 해당 시간 컬럼이 **NULL** (0이면 결함)
  - hourly: 해당 시간 **행 자체가 없어야 함**
  - Verify가 이 규칙대로 자동 채점합니다
- **NaN 행**: 그 시간대 행 중 지정한 개수만 NaN으로 만듭니다(나머지 행은 그대로 goal을 만족).
  제품의 두 통계 경로가 NaN을 다르게 다루는지 실증하기 위한 기능입니다:
  - MV 경로(`daily_stats`)는 필터가 없어 NaN도 세고 평균에 넣습니다 → avg가 NaN이 될 수 있음
  - 폴백 경로(`checkvalue` 직조회)는 `isFinite`로 걸러냅니다 → NaN 제외
  - 그래서 미리보기가 기대값을 **두 벌**로 보여줍니다. 표시값은 폴백 경로 기대이고,
    맨 오른쪽 "NaN · MV 기대" 열이 MV 경로 기대입니다
  - 검증에서 두 경로의 차이는 **결함이 아니라 "경로 차이 관측"**으로 리포트에 따로 실립니다.
    실측이 두 기대 중 어느 쪽과 맞았는지 `matched` 열에 남고, 둘 다 아닐 때만 불일치입니다
  - ⚠️ NaN 주입은 **TestOnly 프로파일 + 주입 화면의 별도 확인 체크박스** 두 겹을 통과해야 실행됩니다.
    NaN이 섞인 (날짜 × cp)는 이후 어떤 검증에서도 클린 기준으로 쓸 수 없습니다

### seed

같은 seed + 같은 조건 = 항상 같은 데이터. 리포트에 남는 seed만 있으면 누구든 같은 검증을 재현할 수 있습니다.

---

## 8. 안전장치 정리 (왜 안 되냐고 묻기 전에)

| 차단/경고 | 이유 |
|---|---|
| INSERT 버튼 비활성 | TestOnly 프로파일이 아니거나, Preview 미실행, 확인 체크 안 함 |
| "보존 창 밖" 실행 차단 | 14일 지난 날짜는 checkvalue 파티션/daily_stats TTL이 만료돼 검증 불가 |
| "복제 지연 N초, 대조 중단" | 복제가 밀린 노드와 비교하면 가짜 불일치 대량 발생 (실측 34만 건) |
| readback "적재 < 계획" | 같은 시나리오 재실행을 ClickHouse가 중복으로 보고 무시함. seed 변경 필요 |
| 재생성이 안 돎 | 해당 날짜 통계가 이미 있으면 스킵(멱등). Regenerate 체크리스트대로 행+마커 삭제 필요 |
| Verify에서 hourly "행 없음" | hourly 테이블은 당일 + 전날 23시만 보관. 그보다 과거 날짜는 행 부재가 정상 |
| INSERT 버튼이 다시 잠김 | 시나리오를 바꾸면 확인한 Preview가 무효화됩니다(확인한 적 없는 대상에 주입되는 것을 막기 위함). Preview를 다시 실행하세요 |
| 주입 후 "readback 실패" 에러 | 주입이 확인되지 않았다는 뜻입니다. 이 상태의 검증 결과는 제품 판정 근거로 쓸 수 없습니다. 로그의 readback 사유를 보고 범위를 정리한 뒤 재주입하세요 |
| 층 결과가 ERROR(대조 불가) | 조회 실패입니다. 테이블명이 배포본과 다르면 1. 연결 > 고급 — 테이블명 오버라이드에서 지정하세요 |
| E2E 실행 중 Generate/Verify 잠김 | E2E 대기 중 같은 (날짜×cp)에 추가 INSERT하면 기대값이 깨져 남은 셀이 전부 오염됨. 중단 후 실행하세요 |
| E2E 시작 전 "사전 점검 차단" | 연결 실패, TestOnly 아님, 전 셀 제외(과거 날짜) 등 — 시작해도 무의미하거나 데이터만 오염되는 상태 |
| 실행 중 창 닫기 확인 대화상자 | 장기 실행 보호. E2E는 상태가 저장되므로 종료해도 "이어서 하기"로 재개 가능 |

---

## 9. 용어 사전

| 용어 | 뜻 |
|---|---|
| checkpoint | MK119의 수집 포인트 하나 ("이 장비의 CPU 사용률" 같은 시계열 단위) |
| checkvalue | ClickHouse의 원본 데이터 테이블. 이 도구가 INSERT하는 곳 |
| MV (Materialized View) | checkvalue에 데이터가 들어오는 순간 자동으로 집계를 갱신하는 ClickHouse 기능. daily_stats(CH)가 이것 |
| daily_stats (MariaDB) | 제품이 최종 저장하는 일별 통계. 사용자 화면이 읽는 곳. 시간 컬럼 `_01`~`_24`는 **한 칸 밀린 종료 라벨** (0시 데이터 → `_01`) |
| hourly_checkpoint_data_log | 제품의 시간별 통계 테이블. hour는 **안 밀린 시작 라벨** (0시 → '00'). daily와 반대라 최대 함정 |
| readback | INSERT 직후 "정말 들어갔나 + 중복은 없나"를 자동 재확인하는 단계 |
| tolerance (1e-6) | MariaDB 경유 비교의 허용오차. 제품이 값을 텍스트로 읽어 다시 숫자로 바꾸는 과정에서 생기는 미세 오차를 흡수 (없으면 절반 이상이 가짜 불일치) |
| max_time | 최댓값이 처음 나온 시각(epoch 밀리초). 동률이면 가장 이른 쪽이 정답 |
| exclude_date | 이 테이블에 등재된 날짜는 hourly가 MV 대신 checkvalue에서 직접 생성됨(폴백) |
| dry-run | DB에 쓰지 않고 생성·미리보기만 |
| TestOnly | 테스트 DB로 본인이 확인한 프로파일에만 켜는 스위치. INSERT의 전제 조건 |

---

## 10. 문제 해결

- **연결 실패**: 방화벽/포트(3306, 9000) 확인. ClickHouse는 HTTP 포트(8123)가 아니라 **native 9000**입니다
- **"Wails 바인딩이 없습니다"**: 브라우저로 index.html을 연 경우. 반드시 rawgen.exe로 실행
- **스키마 확인에서 "컬럼이 없습니다"**: 배포본의 테이블명이 다른 경우. **1. 연결 > 고급 — 테이블명 오버라이드**에서 지정하세요. 직접 편집도 가능 (`%APPDATA%\rawgen\profiles.json`)
- **프로파일/이력 저장 위치**: `%APPDATA%\rawgen\` (profiles.json, history.jsonl, e2e-state.json, reports\). 개명 전 폴더(`statsgenerator`)가 있으면 첫 실행 때 자동으로 옮겨집니다. 비밀번호가 들어 있으니 이 폴더를 공유하지 마세요. 공유가 필요하면 [내보내기] 버튼(비밀번호 자동 제외)을 쓰세요
- **E2E 자동 저장 리포트 위치**: `%APPDATA%\rawgen\reports\e2e-날짜시각.md/.json`

### 재빌드

```powershell
$env:PATH = "C:\Program Files\Go\bin;$env:PATH"
cd C:\Users\Daniell\Documents\ChatGPT\mk119\wails-oqt325
& "$env:USERPROFILE\go\bin\wails.exe" build     # GUI (frontend 포함)
go test ./internal/...                           # 단위 테스트
```
